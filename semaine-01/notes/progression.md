# Progression Semaine 01

## Environnement
- WSL2 + Ubuntu installé
- VS Code connecté à WSL
- Git configuré avec identité pro
- SSH GitHub configuré

## Git Workflow
- Branches : `git checkout -b feat/nom`
- Commit conventionnel : `feat:`, `docs:`, `fix:`
- Pull Request → Merge → Nettoyage branche

## Linux
- Navigation, permissions, utilisateurs
- systemd : gestion des services
- journalctl : lecture des logs
- cron : automatisation planifiée
- Bash scripting : system-check.sh

## Commandes clés
- `systemctl status <service>`
- `journalctl -u <service> -n 20`
- `crontab -e`
- `chmod +x script.sh`
